# Note: You must restart bin/webpack-dev-server for changes to take effect

default: &default
  enabled: true
  host: soak
  port: 8090

development:
  <<: *default

test:
  <<: *default
  enabled: false

production:
  <<: *default
  enabled: false
